sudo: required
services:
  - docker

language: php

php:
  - 7.1
  - 7.2

sudo: false

env:
  - COMPOSER_OPTS=""
  - COMPOSER_OPTS="--prefer-lowest"

before_install:
  - mkdir old_tests/files

install:
  - travis_retry composer update --prefer-dist $COMPOSER_OPTS

before_script:
  - docker run -d --name ftpd_server -p 21:21 -p 30000-30009:30000-30009 -e "PUBLICHOST=localhost" stilliard/pure-ftpd:hardened
  - php wait_for_ftp_service.php
  - docker exec -it ftpd_server sh -c "(echo test; echo test) | pure-pw useradd bob -f /etc/pure-ftpd/passwd/pureftpd.passwd -m -u ftpuser -d /home/ftpusers/bob"

script:
#  - bin/phpunit old_tests/ftp/FtpIntegrationTests.php old_tests/ftp/FtpManualRecursionIntegrationTests.php old_tests/ftp/FtpdIntegrationTests.php old_tests/ftp/FtpdManualRecursionIntegrationTests.php
  - bin/phpunit --coverage-text --coverage-clover coverage.xml
#
#after_script:
#  - wget https://scrutinizer-ci.com/ocular.phar
#  - php ocular.phar code-coverage:upload --format=php-clover coverage.xml
